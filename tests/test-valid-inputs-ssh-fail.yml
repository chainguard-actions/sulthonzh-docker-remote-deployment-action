name: Test valid inputs proceed past validation (fail at SSH)
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Run action with valid inputs but unreachable host
        id: action
        uses: .
        continue-on-error: true
        with:
          remote_docker_host: 'user@192.0.2.1'
          remote_docker_port: '22'
          ssh_public_key: 'ssh-rsa AAAAB3NzaC1yc2EAAAADAQABAAABAQC test@test'
          ssh_private_key: |
            -----BEGIN RSA PRIVATE KEY-----
            MIIEowIBAAKCAQEA0Z3VS5JJcds3xHn/ygWep4PAtEsHAcGCMCBBFMhFqJFBqBqS
            -----END RSA PRIVATE KEY-----
          args: 'up -d'
          deployment_mode: 'docker-compose'
          stack_file_name: 'docker-compose.yml'
          keep_files: '4'
          docker_prune: 'false'
          pull_images_first: 'false'

      - name: Assert action failed (expected - unreachable SSH host, not validation error)
        env:
          OUTCOME: ${{ steps.action.outcome }}
        run: |
          echo "outcome: $OUTCOME"
          # The action should fail because the SSH host is unreachable,
          # but it should have passed input validation.
          # We just verify the action ran and failed (not skipped or cancelled).
          if [ "$OUTCOME" != "failure" ]; then
            echo "FAIL: expected action to fail at SSH stage, but got: $OUTCOME"
            exit 1
          fi
          echo "PASS: action correctly failed at SSH/connection stage (input validation passed)"
